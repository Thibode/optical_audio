{% extends 'layouts/base.html.twig' %}

{% block title %}Ajout Opticien{% endblock %}

{% block body %}
    <div class="thirdPart">
        <h2 id="ajoutOpticien">Ajout Opticien</h2>

        {{ form_start(formOpticien) }}
            <div class="form-position-opt">
            <div>
                {{ form_label(formOpticien.lastname, 'Nom') }}
                {{ form_widget(formOpticien.lastname) }}
            </div>
            <div>
                {{ form_label(formOpticien.firstname, 'Prénom') }}
                {{ form_widget(formOpticien.firstname) }}
            </div>
            </div>
            <div class="btn_s fill">
                <input type="submit" value="Ajouter" class="btn">
            </div>
        {{ form_end(formOpticien) }}
    </div>
    <div class="RecapitulatifCollaborateur">
    {% if opticiens != null %}
        <h2 class="titreTable">Recapitulatif Collaborateur</h2>
        {{ include('/opticiens/table.html.twig') }}
    {% else %}

        <h3 class="noTable" id="tableClient">Il n'y a pas encore de collaborateur</h3>

    {% endif %}
    </div>
{% endblock %}
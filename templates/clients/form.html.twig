<div class="firstPart">
    <h2 id="ajoutClient">Ajout Client</h2>

    {{ form_start(form) }}
        <div class="enteteForm">
            {{ form_row(form.lastname) }}
            {{ form_row(form.firstname) }}
        </div>
        <div class="selectForm">
            {{ form_row(form.email) }}
            {{ form_row(form.opticien)}}
        </div>
        <div class="selectForm">
            {{ form_row(form.dateTest) }}
            {{ form_row(form.appareil) }}
        </div>
        <div class="selectForm">
            {{ form_row(form.perteAuditive) }}
            {{ form_row(form.deni) }}
        </div>
        <div class="selectForm">
            {{ form_row(form.souhait) }}    
        </div>
        <div class="autreForm">
            {{ form_row(form.remarque) }}
        </div>
        <div class="btn_s fill">
            <input type="submit" value="Ajout Client" class="btn">
        </div>
    {{ form_end(form) }}
</div> 